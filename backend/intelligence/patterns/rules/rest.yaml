name: REST API
category: COMMUNICATION
type: REST

participants:
  route:
    type: ROUTE
  handler:
    type: FUNCTION

relationships:
  - source: handler
    target: route
    type: EXPOSES

confidence:
  base: 0.8
