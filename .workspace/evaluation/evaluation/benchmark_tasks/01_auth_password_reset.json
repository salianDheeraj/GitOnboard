{
  "task_id": "TASK-001",
  "category": "REQUIREMENT_OMISSION",
  "title": "Password Reset Token Expiration",
  "repository_target": "default",
  "base_commit": "main",
  "prompt": "Implement password reset token generation and validation. Tokens must expire 15 minutes after issuance and be rejected if expired or re-used.",
  "expected_contract": {
    "required_endpoints": ["POST /api/auth/reset-request", "POST /api/auth/reset-confirm"],
    "expected_components": ["src/pages/api/auth/reset.ts"],
    "invariants": ["Expiration timestamp (15 min) stored in DB", "Expiration check before updating password"],
    "required_tests": ["Unit test verifying token expiry case"]
  },
  "known_pitfall_check": {
    "type": "CONTRACT_INVARIANT",
    "missing_pattern": "expir"
  },
  "seeded_failure_mode": "Requirement Hallucination: Omits token 15-minute expiration check."
}
